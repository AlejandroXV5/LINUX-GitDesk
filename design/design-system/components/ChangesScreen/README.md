The full window with every shell piece stacked in its real order — title bar, menu bar, toolbar, then a sidebar/main split that fills the remaining height, then the status bar pinned to the bottom — showing the Commit workflow: BranchTree on the left, ChangesPanel on the right. This is what "Commit" in the menu bar and "Changes" in the toolbar both land on. Only `.body` and its two children scroll or grow; every other band (title bar 38px, menu bar 32px, toolbar 44px, status bar 24px) is a fixed height, so resizing the window only ever changes how much sidebar and changes-list a developer sees, never the chrome around it. Use the toggle in the corner to check both themes hold their contrast at full assembly, not just in an isolated component swatch.
