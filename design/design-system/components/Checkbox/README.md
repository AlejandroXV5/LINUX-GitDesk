A 16px box, `border-control` edge, `radius-sm` — checked state fills solid `brand-solid` with an `ink-inverse` check mark rather than tinting the box, so it reads correctly even for a color-blind viewer at a glance (shape changes, not only color). Used sparingly: today only for `Amend` under the commit-message box. Disabled drops both the box border and the label to their disabled tokens together, never one without the other.
